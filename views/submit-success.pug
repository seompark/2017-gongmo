extends page

append contents
  .hero.is-primary
    .hero-body(style='min-height: 100vh;')
      .container.has-text-centered
        .box.is-flex(style=`
          min-height: 60vh;
          flex-direction: column;
          justify-content: center;
          align-items: center;
        `)
          p.title(style='color: #cccccc')
            | (〃´∀｀)
          p.subtitle.has-text-black(style='margin-top: 0px')
            | 제출되었습니다. 접수 마감 전까지는 언제든지 수정 가능합 니다.
          a.button.is-medium(href="/")
            | 메인 메뉴로 돌아가기